name: wallet_demo
description: Wallet reference app for the Mosaic design system.
publish_to: none

version: 0.1.0+1

environment:
  sdk: ^3.10.0
  flutter: ">=3.38.0"

dependencies:
  flutter:
    sdk: flutter
  mosaic_ui:
    git:
      url: git@github.com:mosaic-de/mosaic.git
      path: packages/mosaic_ui
  shared_preferences: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: false
